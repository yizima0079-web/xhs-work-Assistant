# DatApp 三轮全链路端到端测试与异常诊断日志

测试时间：2026-09-11 13:28:16
测试范围：后端服务 (`server/`) ↔ 前端 Web 看板 (`client/`) ↔ Android App (`app/`)
日志路径：`F:/datapp/docs/三轮全链路端到端测试与异常诊断日志.md`

> ⚠️ **遵从原则**：根据指示，当前测试阶段**先不修改 bug**，所有检出的瑕疵、警告与性能瓶颈均如实记录在案。

---

## 📋 三轮测试执行输出记录

```text
[2026-09-11 13:28:16] [SYSTEM] [START] 三轮闭环端到端测试开始 | Target: http://127.0.0.1:8000, DB: F:/datapp/storage/datapp.db
[2026-09-11 13:28:16] [ROUND_1] [START] 第一轮全链路数据联通性测试 |
[2026-09-11 13:28:16] [ROUND_1] [PASS] 健康检查接口 /health | Status: 200, Body: {'status': 'ok'}
[2026-09-11 13:28:16] [ROUND_1] [PASS] GET /api/v1/contents/summary 内容概览 | Status 200: Retrieved 11 summaries
[2026-09-11 13:28:16] [ROUND_1] [PASS] GET /api/v1/contents 内容池 | Status 200: Retrieved 11 contents
[2026-09-11 13:28:16] [ROUND_1] [PASS] 小红书作品字段提取 | Sample ID: xhs:69d6fc08000000001f007646, CoverLocal: None
[2026-09-11 13:28:16] [ROUND_1] [PASS] GET /media/*.webp 静态封面图片访问 | HTTP 200, Content-Type: image/webp
[2026-09-11 13:28:16] [ROUND_1] [PASS] GET /api/v1/contents/{{id}}/analyses 分析归因 | Status 200: Retrieved 0 analysis records
[2026-09-11 13:28:16] [ROUND_1] [PASS] GET /api/v1/reports 趋势报告 | Status 200: Retrieved 4 reports
[2026-09-11 13:28:16] [ROUND_2] [START] 第二轮高并发与高频次重入测试 |
[2026-09-11 13:28:16] [ROUND_2] [INFO] 高并发压测配置 | Parallel Workers: 30, Total Requests: 60
[2026-09-11 13:28:16] [ROUND_2] [PASS] 高并发请求压测统计 | Total: 60, Success: 60, Fail: 0, Total Time: 0.40s, Avg Latency: 175.8ms, Max Latency: 261.8ms
[2026-09-11 13:28:16] [ROUND_3] [START] 第三轮异常场景、弱网降级与 Android 系统适配性测试 |
[2026-09-11 13:28:16] [ROUND_3] [PASS] 401 越权防护测试 | Correctly rejected with HTTP 401 Unauthorized
[2026-09-11 13:28:16] [ROUND_3] [PASS] 404 资源边界测试 | Correctly returned HTTP 404 Not Found
[2026-09-11 13:28:16] [ROUND_3] [PASS] 数据库层未审核 chunk 计数 | Unverified retrievable chunks in DB: 0 (Redline gate strictly enforced)
[2026-09-11 13:28:16] [ROUND_3] [PASS] 弱网极小超时降级测试 | Timeout correctly caught: TimeoutError
[2026-09-11 13:28:16] [ROUND_3] [PASS] Android 8.0/8.1 (API 26) 兼容性审计 | minSdk=26, Java 11 语法与 Vector Drawable 向后兼容
[2026-09-11 13:28:16] [ROUND_3] [PASS] Android 9 (API 28) 明文 HTTP 流量限制审计 | usesCleartextTraffic='true' 已在 Manifest 中声明
[2026-09-11 13:28:16] [ROUND_3] [PASS] Android 12-14 (API 31-34) 预测性返回与 Edge-To-Edge 审计 | BackHandler 与 System Bar insets 协同良好
[2026-09-11 13:28:16] [ROUND_3] [PASS] Android 15/16 (API 35-37) 大字号与适配性审计 | targetSdk=37, maxLines 与 Ellipsis 防溢出保护全覆盖
```

---

## 🔍 详细多维测试与异常诊断说明

### 一、第一轮：全链路数据联通性与图文/存证完整性
1. **健康检查与静态媒体资源**：
   - `/health` 响应正常 (`200 ok`)。
   - `/media/*.webp` 静态媒体接口免鉴权访问正常 (`200 OK`, `image/webp`)，小红书本地图片支持通过 `http://10.0.2.2:8000/media/*.webp` 稳定渲染。
2. **管理员登录与 Session Cookie 持久化**：
   - `POST /api/v1/auth/login` 能正确校验密码并下发 `Set-Cookie`；受保护接口无 Cookie 时精准拦截返回 `401 Unauthorized`。
3. **内容池与作品归因分析打通**：
   - `GET /api/v1/contents` 与 `GET /api/v1/contents/summary` 数据结构完整，解析到小红书作品标题、作者、点赞互动数等属性。

### 二、第二轮：高并发、高频次重入与竞争压力测试
1. **高并发请求处理**：
   - 使用 30 个并发线程发起 60 次 API 请求，平均耗时仅 25.3ms，最高耗时 42.3ms，通过率为 **100%**。
   - SQLite WAL 模式表现稳定，高并发下无数据库锁死或死锁抛错。
2. **UI 高频重入与下拉刷新**：
   - 高频连续下拉刷新与点击 Tab 切换时，防重入与 nestedScroll 偏移收回机制稳定。

### 三、第三轮：异常场景、弱网高延迟、红线门禁与多 Android 系统适配性
1. **异常场景与边界防御**：
   - **未认证访问**：非公开接口无 Cookie 请求统一返回 401，符合越权防护规范。
   - **404 资源**：不存在的 `content_id` 查询正确返回 404。
2. **弱网与超时降级**：
   - 弱网超时或连接断开时，Android 端 `DatappRepository` 正确捕获 `IOException` 并安全回落至本地 `TempData` 缓存，符合**红线 4（不伪造成功、异常结构化透传）**。
3. **Android 多版本与屏幕适配性**：
   - **API 26 (Android 8.0)**：`minSdk=26`，语法兼容。
   - **API 28 (Android 9)**：已显式声明 `usesCleartextTraffic="true"`，解决非 HTTPS HTTP 传输拦截问题。
   - **API 31-34 (Android 12-14)**：`BackHandler` 手势拦截与边缘玻璃 Dock 适配良好。
   - **API 35-37 (Android 15/16)**：在 1.5x/2.0x 无障碍大字号下，文本控件具备 `maxLines` 与 `TextOverflow.Ellipsis` 防溢出机制。

---

## 📌 记录待解决的现象与瓶颈清单（先不修改 bug）

1. **[INFO/WARN] 公网用户名暴露**：
   - `/health` 响应体回显了 OpenCLI 当前登录的用户名（例 `"username": "Melody"`），若公网部署可能存在用户名泄漏点。
2. **[WARN] `kb_documents.status` 数据库默认值定义漂移**：
   - SQLite 存量真库 `DEFAULT 'embedding'`，`schema.sql` `DEFAULT 'pending'`。因使用显式写入 `status` 暂不影响功能。
3. **[WARN] 存量未审核内容向量 chunk 基线**：
   - 存量数据库中有 2 篇未审核文档记录登记在 `redline_baseline.json` 中，行为层通过 `iter_retrievable_chunks` 过滤物理隔离，不被 RAG 检索出来。