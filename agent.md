# Agent 边界限制

AI 编码 / 自动化 agent 在本项目的硬性约束。违反任一条即停止并上报，不得自行变通。

## 1. 采集安全红线

- 只采集公开或用户明确授权的数据，范围 = 授权账号可见内容。
- **禁止绕过**验证码、登录墙、风控、技术防护；禁止伪造 UA / 指纹规避检测。
- 采集低频少量：全局并发 = 1，每平台独立令牌桶，请求间带上限抖动（不用于规避检测）。
- 429 / 403 / 验证码 / 登录异常 / 300017 / 300031 → 立即熔断，禁止盲目重试。
- 连续失败达阈值进入 `blocked`，人工确认后才能恢复。
- 重试仅限临时网络错误；认证失败 / 权限失败 / 风控信号不重试。
- 单任务限制最大条数 / 媒体大小 / 运行时长；任务取消后不自动重试。
- Cookie / Token / Authorization / 签名参数不写入代码、日志、报告或普通导出，只经安全配置或受控凭证存储注入。
- 含 `xsec_token` 的来源 URL 必须脱敏保存；复现所需敏感参数不得进入业务表。
- 外部网络仅用于明确的仓库、依赖和 API 验证；禁止 `curl|bash`、远程脚本管道和未知安装脚本。
- 依赖仅安装到项目目录，未经授权禁止全局安装。

## 2. 数据边界

- 三层模型：Raw（原始响应 / 快照，不改写）/ Normalized（标准化）/ Curated（审核通过）。
- 原始层保留：采集时间、脱敏来源 URL、适配器版本、源码包版本或 commit、SHA-256、MIME。
- 互动数据（点赞 / 评论）是快照，必须带采集时间。
- 媒体默认只存元数据 + 引用；仅授权时下载；媒体文件与数据库分离。
- 默认全局并发 = 1；单平台请求间隔 5–30 秒；单任务最多 10 条、最长 10 分钟；媒体大小必须限额。
- 日志脱敏：个人信息 / Cookie / Authorization / 手机号。
- `contents(platform, platform_item_id)` 唯一；媒体按内容哈希去重。

## 3. 事实边界

- 模型输出 ≠ 事实来源；信息源审核 = 多轮（提取 → 拆 Claim → 交叉验证 → 来源分级 → 人工复核）。
- 审核状态由规则 + 证据 + 人工复核共同决定，不直接由 LLM 决定。
- 报告双格式：先机器可校验 JSON，再渲染 Markdown；禁止只出不可审计的自然语言。
- 爆点 / 人设 / 趋势必须来自内容样本 + 指标，附证据和反例；样本不足输出「不确定」。
- Agent 回答必须带来源引用 + 时间范围 + 可信状态 + 无法确定的部分。
- 失败任务保留输入 / 状态 / 错误分类，用原 `run_id` 重放，禁止手工改库伪造成功。

## 4. 明确禁止

1. 绕过平台风控 / 验证码 / 登录。
2. 硬编码凭证，或把凭证写进日志 / 报告。
3. 把模型输出当事实写入高可信知识库。
4. 无标记把 `lead`（线索）混入 `verified` 检索。
5. 手工改库伪造采集 / 审核成功。
6. 高频升压测试触发平台风控。
7. 把第三方平台内容重新包装成无来源原创事实。
8. 采集超出授权范围的数据。
