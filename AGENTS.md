# Agent 边界限制

本项目 AI 编码与自动化 Agent 的硬性约束。违反任一条即停止并上报，不得自行变通。

## 采集安全

- 只采集公开或用户明确授权的数据，范围限定为授权账号可见内容。
- 禁止绕过验证码、登录墙、风控和技术防护；禁止伪造 UA、指纹或请求行为规避检测。
- 全局并发默认 1；每个平台独立令牌桶；请求间隔默认 5–30 秒，不得用于规避检测。
- 429、403、验证码、登录异常、300017、300031 立即熔断，禁止盲目重试。
- 连续失败达到阈值进入 `blocked`，人工确认后才能恢复。
- 仅临时网络错误允许重试；认证失败、权限失败、风控信号禁止重试。
- 单任务默认最多 10 条、最长 10 分钟；限制媒体大小；任务取消后不自动重试。

## 凭证与敏感数据

- Cookie、Token、Authorization、签名参数不得写入代码、日志、报告、普通导出或提交记录。
- 含 `xsec_token` 的 URL 必须脱敏保存；复现所需敏感参数只能进入受控凭证存储。
- 日志和报告脱敏个人信息、账号标识、手机号及完整来源查询参数。
- 外部网络仅用于明确的仓库、依赖和 API 验证；不执行 `curl|bash`、远程脚本管道或未知安装脚本。
- 依赖安装限定在项目目录，禁止未经授权的全局安装。

## 数据与事实

- 三层模型：Raw（原始快照，不改写）/ Normalized（标准化）/ Curated（审核通过）。
- 原始层保留采集时间、脱敏来源 URL、适配器版本、源码包版本或 commit、SHA-256、MIME。
- 互动数据是带采集时间的快照；`contents(platform, platform_item_id)` 唯一；媒体按内容哈希去重。
- 模型输出不等于事实；审核必须经过 Claim 拆分、多源交叉验证、来源分级和人工复核。
- 报告先生成可校验 JSON，再渲染 Markdown；爆点、人设、趋势必须附样本、指标、证据和反例。
- Agent 回答必须带来源引用、时间范围、可信状态和不确定部分。
- 失败任务保留输入、状态和错误分类，使用原 `run_id` 重放，禁止手工改库伪造成功。

## 明确禁止

1. 绕过平台风控、验证码、登录或授权范围。
2. 将模型输出无证据写入高可信知识库。
3. 无标记把 `lead` 混入 `verified` 检索。
4. 高频升压测试触发平台风控。
5. 把第三方内容包装成无来源原创事实。
